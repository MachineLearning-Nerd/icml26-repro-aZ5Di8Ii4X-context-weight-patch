# Limitations and deviations

- One pretrained Gemma 3 1B checkpoint and one prompt/token are tested.
- The public ungated mirror is used; its weight blob ID and byte size match the gated official checkpoint exactly.
- The experiment tests float32 numerical realization of exact real-arithmetic theorems.
- It does not establish a reusable patch for arbitrary future tokens.
